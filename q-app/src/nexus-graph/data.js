// Default demo dataset for the Nexus graph component.
// Pass your own `categories`, `people`, and `edges` props to <NexusGraph /> to override.
// Edit the JSON files in ./data/ to change the default dataset.

import categories from './data/categories.json'
import people from './data/people.json'
import edges from './data/edges.json'

export const DEFAULT_CATEGORIES = categories
export const DEFAULT_PEOPLE = people
export const DEFAULT_EDGES = edges
