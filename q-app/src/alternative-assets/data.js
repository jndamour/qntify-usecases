// Edit the JSON files in ./data/ to change the page's content.

import relationshipNodes from './data/relationship-nodes.json'
import relationshipEdges from './data/relationship-edges.json'
import lifecycle from './data/lifecycle.json'
import cases from './data/cases.json'

export const ALT_DATA = {
  relationshipNodes,
  relationshipEdges,
  lifecycle,
  cases,
}
